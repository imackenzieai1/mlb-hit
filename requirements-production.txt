# Pinned production environment for the cloud daily runner.
#
# IMPORTANT: scikit-learn is pinned to 1.7.2 because the xgb_v3 model bundle
# was pickled with that version. Using 1.8.x triggers an unpickle warning and
# can break the isotonic calibrator silently. Bump only after retraining.
#
# Python: 3.11 (matches pyproject.toml requires-python and the GitHub Actions
# workflow's actions/setup-python@v5 with python-version: '3.11').

pandas==2.2.3
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.7.2
xgboost==2.1.4
pybaseball==2.2.7
MLB-StatsAPI==1.7.2
requests==2.32.3
python-dotenv==1.0.1
pyarrow==17.0.0
joblib==1.4.2
pyyaml==6.0.2
tqdm==4.66.5
pytz==2024.1
